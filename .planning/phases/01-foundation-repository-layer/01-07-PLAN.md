---
phase: 01-foundation-repository-layer
plan: 07
type: execute
wave: 4
depends_on: ["01-01", "01-02", "01-03", "01-04", "01-05", "01-06"]
files_modified:
  - src/cli.ts
  - src/commands/review.ts
  - src/commands/init.ts
  - src/commands/doctor.ts
  - src/commands/index.ts
  - src/commands/review.test.ts
  - src/commands/init.test.ts
  - src/commands/doctor.test.ts
autonomous: true
requirements: ["REPO-01", "REPO-02", "REPO-03", "REPO-04", "CONF-01", "CONF-02", "CACHE-01", "CACHE-02", "CACHE-03", "MODEL-02", "OUT-02"]
user_setup: []
must_haves:
  truths:
    - "octate review [ref...] --staged --working --commit --range --json --sarif --quiet works end-to-end"
    - "octate init creates valid octate.yaml with commented examples at repository root"
    - "octate doctor validates config, Git repo access, NVIDIA connectivity, cache health"
    - "Global options: --config, --cache-dir, --log-level, --debug, --version, --help"
    - "Exit codes map correctly: 0=passed, 1=blocking findings, 2=config error, 3=repo/Git error, 4=model error, 5=internal error"
    - "Output modes mutually exclusive: --json | --sarif | --quiet | default TUI"
    - "Ctrl+C gracefully cancels all operations without orphaned processes"
  artifacts:
    - path: "src/cli.ts"
      provides: "CLI entry point with Commander.js setup"
      exports: ["main", "program"]
    - path: "src/commands/review.ts"
      provides: "Review command with all scope and output flags"
      exports: ["reviewCommand"]
    - path: "src/commands/init.ts"
      provides: "Init command implementation"
      exports: ["initCommand"]
    - path: "src/commands/doctor.ts"
      provides: "Doctor command with diagnostics"
      exports: ["doctorCommand"]
    - path: "src/commands/index.ts"
      provides: "Command registry"
      exports: ["registerCommands"]
  key_links:
    - from: "src/cli.ts"
      to: "src/commands/review.ts"
      via: "registers review command"
      pattern: "program.command('review')"
    - from: "src/commands/review.ts"
      to: "src/repository/scope.ts"
      via: "resolves scope via resolveScope"
      pattern: "resolveScope"
    - from: "src/commands/review.ts"
      to: "src/config/merger.ts"
      via: "loads config with precedence"
      pattern: "loadConfig"
    - from: "src/commands/doctor.ts"
      to: "src/cache/store.ts"
      via: "checks cache health"
      pattern: "CacheStore"
    - from: "src/cancellation/controller.ts"
      to: "src/commands/review.ts"
      via: "wraps review in cancellation"
      pattern: "withCancellation"
</threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI Args → Commands | User input for refs, flags, options; validated by Commander.js and custom logic |
| Config Files → Commands | Loaded via config merger; validated by Zod schema |
| File System → Commands | Reads/writes repo files, cache, config; user-controlled paths |
| Process Signals → Cancellation | SIGINT (Ctrl+C) triggers abort; handled by process.on |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-24 | Tampering | CLI Args | mitigate | Commander.js validation; explicit flag definitions; mutual exclusion groups |
| T-01-25 | Tampering | Config Override | mitigate | Precedence chain enforced; CLI highest; env vars parsed with validation |
| T-01-26 | Denial of Service | Long-Running Review | mitigate | Cancellation via AbortController; timeout options; progress streaming |
| T-01-27 | Information Disclosure | Debug Logs | mitigate | --debug only shows structure; secrets redacted by logger; no source in debug |
| T-01-28 | Elevation of Privilege | File System Access | mitigate | Path resolution validates within repo root; no arbitrary file read/write |
</threat_model>

<verification>
- pnpm test passes for all command tests
- octate review --help shows all commands and options
- octate review --staged produces diff for staged changes
- octate review HEAD~1 produces diff for commit range
- octate review --json outputs valid JSON
- octate review --sarif outputs valid SARIF (structure only, Phase 7 implements renderer)
- octate review --quiet outputs minimal summary
- octate init creates octate.yaml at repo root with commented examples
- octate doctor reports: config valid, Git accessible, cache size, NVIDIA connectivity (local mode)
- Exit codes: 0 (success), 2 (config error), 3 (Git error), 5 (internal error) verified
- Ctrl+C during review cancels cleanly (no orphaned processes)
- Global options work: --config, --cache-dir, --log-level, --debug, --version, --help
</verification>

<success_criteria>
- Commander.js command structure per D-26, D-27, D-28, D-29, D-30
- review command: positional [ref...] variadic, --staged, --working, --commit, --range, --json, --sarif, --quiet
- init command: creates octate.yaml with full schema defaults and comments (D-109)
- doctor command: validates config syntax, Git repo access, NVIDIA connectivity (local), cache health (D-110)
- Exit code mapping per D-24, D-25 and ROADMAP Phase 6
- Output modes mutually exclusive (--json | --sarif | --quiet | TUI) per D-30
- Cancellation integrated: Ctrl+C triggers AbortController propagated to all layers
- All command tests pass
- End-to-end: octate review works in a real Git repository
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-repository-layer/01-07-SUMMARY.md`
</output>