---
phase: 01-foundation-repository-layer
plan: 04
type: execute
wave: 2
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - src/repository/ignore.ts
  - src/repository/filter.ts
  - src/repository/index.ts
  - src/repository/ignore.test.ts
  - src/repository/filter.test.ts
autonomous: true
requirements: ["REPO-03"]
user_setup: []
must_haves:
  truths:
    - ".gitignore patterns are respected during file discovery and diff generation"
    - ".octateignore patterns extend .gitignore with project-specific ignores"
    - "Binary files are detected and excluded from analysis"
    - "Generated files (minified, source maps, generated code) are detected and excluded"
    - "Monorepo workspaces each contribute their own ignore patterns (union)"
    - "Symlinks, large files, vendor dirs, node_modules, build output handled safely"
  artifacts:
    - path: "src/repository/ignore.ts"
      provides: "Ignore pattern parsing and matching (.gitignore, .octateignore)"
      exports: ["IgnoreMatcher", "parseIgnoreFile", "createIgnoreMatcher"]
    - path: "src/repository/filter.ts"
      provides: "File filtering: binary detection, generated file heuristics, size limits"
      exports: ["isBinaryFile", "isGeneratedFile", "shouldAnalyzeFile", "FileFilter"]
    - path: "src/repository/index.ts"
      provides: "Updated public API with ignore and filter exports"
      exports: ["IgnoreMatcher", "FileFilter", "isBinaryFile", "isGeneratedFile"]
  key_links:
    - from: "src/repository/ignore.ts"
      to: "ignore package"
      via: "uses ignore package for .gitignore parsing"
      pattern: "import.*from 'ignore'"
    - from: "src/repository/filter.ts"
      to: "src/repository/ignore.ts"
      via: "uses IgnoreMatcher for pattern matching"
      pattern: "IgnoreMatcher"
    - from: "src/repository/git.ts"
      to: "src/repository/filter.ts"
      via: "filters files before diff/analysis"
      pattern: "shouldAnalyzeFile"
</threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File System → Filter | Reads file contents for binary/generated detection; user-controlled files |
| Ignore Files → Matcher | Reads .gitignore, .octateignore from repo; user-controlled patterns |
| Config Ignore → Filter | Config ignore patterns from octate.yaml; user-controlled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-12 | Tampering | Ignore Patterns | mitigate | Union of all patterns; explicit deny list for critical dirs (.git, node_modules) |
| T-01-13 | Information Disclosure | Binary/Generated Files | mitigate | Detect and exclude before sending to analysis/model; never process binary content |
| T-01-14 | Denial of Service | Large File Reads | mitigate | Size limit check before reading; streaming for large files; max file size config |
| T-01-15 | Spoofing | Symlink Traversal | mitigate | Resolve symlinks; reject if target outside repository root |
</threat_model>

<verification>
- pnpm test passes for all ignore and filter tests
- .gitignore patterns correctly exclude node_modules, dist, build, *.log
- .octateignore adds project-specific ignores on top of .gitignore
- Binary files (images, compiled) detected via UTF-8 check and excluded
- Generated files detected: // Generated, /* eslint-disable */, minified patterns, .map files
- Monorepo: root ignore patterns apply everywhere; workspace .octateignore unioned
- Symlinks resolved; files outside repo root rejected
- Large files (>10MB default) skipped with warning
</verification>

<success_criteria>
- IgnoreMatcher correctly parses .gitignore and .octateignore using ignore package
- FileFilter integrates ignore patterns + binary detection + generated file heuristics + size limits
- Monorepo ignore union works: root patterns + each workspace's patterns (D-23)
- Binary detection uses Buffer.isUtf8() or similar; excludes non-UTF8 files
- Generated file heuristics catch common patterns (minified, source maps, generated headers)
- Config ignore patterns from octate.yaml merged with file-based ignores
- Files in .git, node_modules, vendor, build output always excluded regardless of config
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-repository-layer/01-04-SUMMARY.md`
</output>