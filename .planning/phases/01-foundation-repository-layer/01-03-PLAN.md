---
phase: 01-foundation-repository-layer
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - src/repository/discovery.ts
  - src/repository/git.ts
  - src/repository/scope.ts
  - src/repository/monorepo.ts
  - src/repository/index.ts
  - src/repository/discovery.test.ts
  - src/repository/git.test.ts
  - src/repository/scope.test.ts
  - src/repository/monorepo.test.ts
autonomous: true
requirements: ["REPO-01", "REPO-02", "REPO-04"]
user_setup: []
must_haves:
  truths:
    - "octate review auto-discovers Git repository root from any subdirectory"
    - "octate review --staged produces correct Git diff with base, head, changed files, changed lines"
    - "octate review HEAD~1 produces correct Git diff for commit range"
    - "Monorepo detection works for pnpm, npm/yarn, Turborepo, Nx"
    - "ReviewScope model includes full unified diff with line ranges"
  artifacts:
    - path: "src/repository/discovery.ts"
      provides: "Git root discovery, workspace detection"
      exports: ["discoverRepository", "findGitRoot", "detectWorkspace"]
    - path: "src/repository/git.ts"
      provides: "Git operations: diff, status, rev-parse, log"
      exports: ["getDiff", "getStatus", "resolveRef", "getLog", "GitError"]
    - path: "src/repository/scope.ts"
      provides: "ReviewScope model and scope resolution"
      exports: ["resolveScope", "ReviewScope", "ScopeOptions"]
    - path: "src/repository/monorepo.ts"
      provides: "Monorepo detection and workspace aggregation"
      exports: ["detectMonorepo", "aggregateWorkspaces", "MonorepoType"]
    - path: "src/repository/index.ts"
      provides: "Public repository API"
      exports: ["Repository", "discoverRepository", "resolveScope", "getDiff"]
  key_links:
    - from: "src/repository/scope.ts"
      to: "src/repository/git.ts"
      via: "calls getDiff for scope resolution"
      pattern: "getDiff\\(base, head\\)"
    - from: "src/repository/discovery.ts"
      to: "src/repository/monorepo.ts"
      via: "delegates monorepo detection"
      pattern: "detectMonorepo\\(repoRoot\\)"
    - from: "src/repository/git.ts"
      to: "isomorphic-git"
      via: "uses isomorphic-git APIs"
      pattern: "from 'isomorphic-git'"
</threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Git Operations → File System | Reads .git directory, executes git commands via isomorphic-git |
| Ref Parsing → User Input | User provides refs (HEAD~1, branch names, SHAs); must validate |
| Diff Generation → Repository Content | Reads file contents for diff; large/binary files handled safely |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08 | Tampering | Ref Resolution | mitigate | Validate all refs with isomorphic-git; fail fast with suggestions (D-02) |
| T-01-09 | Information Disclosure | Diff Output | mitigate | Diff only includes changed files; respect ignore patterns (D-45) |
| T-01-10 | Denial of Service | Large Repo Diff | mitigate | Bound diff size; stream large diffs; reject binary files |
| T-01-11 | Spoofing | Git Root Discovery | mitigate | Verify .git directory exists and is valid; don't follow symlinks outside repo |
</threat_model>

<verification>
- pnpm test passes for all repository tests
- discoverRepository finds .git root from any subdirectory
- getDiff produces unified diff with correct base, head, file list, line ranges
- resolveScope handles --staged, --working, --commit=<ref>, --range=<base>..<head>
- detectMonorepo identifies pnpm-workspace.yaml, package.json workspaces, turbo.json, nx.json
- Invalid refs fail fast with descriptive error showing valid refs
</verification>

<success_criteria>
- Repository discovery works from any subdirectory (REPO-04)
- Git diff generation produces ReviewScope with base, head, changed files, changed lines (REPO-02)
- Scope flags (--staged, --working, --commit, --range) mutually exclusive and functional (D-04, D-28)
- Monorepo detection priority: pnpm → npm/yarn → turbo → nx (D-21)
- Nested workspaces supported with aggregated ignore patterns (D-22, D-23)
- Three-dot range (A...B) parsed correctly (D-28)
- All ref formats supported: HEAD~1, origin/main...HEAD, branch names, SHAs, tags (D-01)
- Invalid/ambiguous refs fail fast with suggestions (D-02)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-repository-layer/01-03-SUMMARY.md`
</output>