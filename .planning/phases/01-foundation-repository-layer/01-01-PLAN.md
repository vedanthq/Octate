---
phase: 01-foundation-repository-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - tsconfig.json
  - .gitignore
  - biome.json
  - jest.config.ts
  - src/types/index.ts
  - src/errors/index.ts
  - src/logging/index.ts
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "Project builds and tests run with pnpm"
    - "TypeScript compiles with strict mode"
    - "Biome linting and formatting works"
    - "Jest test framework executes"
  artifacts:
    - path: "package.json"
      provides: "Project metadata, scripts, dependencies"
      min_lines: 50
    - path: "tsconfig.json"
      provides: "Strict TypeScript configuration"
      contains: "strict: true"
    - path: "biome.json"
      provides: "Lint and format configuration"
    - path: "src/types/index.ts"
      provides: "Core domain type definitions"
      exports: ["Repository", "Workspace", "MonorepoConfig", "FileChange"]
    - path: "src/errors/index.ts"
      provides: "Typed error hierarchy with exit codes"
      exports: ["OctateError", "ConfigurationError", "RepositoryError", "GitError", "ParseError", "AnalysisError", "ContextError", "ModelError", "ProviderRateLimitError", "ProviderTimeoutError", "AuthenticationError", "QuotaExceededError", "ValidationError", "InternalError"]
    - path: "src/logging/index.ts"
      provides: "Pino logger with child logger support"
      exports: ["createLogger", "logger"]
  key_links:
    - from: "package.json"
      to: "tsconfig.json"
      via: "build script"
      pattern: '"build": "tsc"'
    - from: "src/types/index.ts"
      to: "src/errors/index.ts"
      via: "import"
      pattern: "import.*from.*types"
---

<objective>
Create the project scaffold with build tooling, core types, error hierarchy, and logging infrastructure.

Purpose: Establish the foundation that all subsequent plans depend on — project structure, TypeScript configuration, linting, testing, and core domain primitives (types, errors, logging).

Output: Working project that compiles, lints, and runs tests; core types and errors available for import.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-repository-layer/01-CONTEXT.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>task 1: Create project scaffold with package.json, pnpm workspace, and TypeScript config</name>
  <files>
    package.json
    pnpm-workspace.yaml
    tsconfig.json
    .gitignore
  </files>
  <action>
Create the project scaffold following the tech stack from STACK.md:

1. **package.json** with:
   - name: "octate", version: "0.1.0", private: true, type: "module"
   - engines: { "node": ">=22.0.0" }
   - scripts: "build": "tsc", "dev": "tsx watch src/cli.ts", "test": "jest", "test:watch": "jest --watch", "lint": "biome check .", "format": "biome format --write .", "check": "biome check --apply .", "prepare": "husky install"
   - dependencies: commander@15.0.0, isomorphic-git@1.41.9, yaml@2.9.0, zod@4.5.4, pino@10.3.1, p-limit@7.3.2, p-queue@7.3.2, fast-glob@3.3.3, ignore@7.0.8, picocolors@1.1.1, @microsoft/sarif-sdk@latest, node-sarif-builder@5.0.0
   - devDependencies: typescript@7.0.2, tsx@latest, @types/node@26.4.1, @types/react@latest, @types/jest@latest, jest@30.5.1, ts-jest@29.4.12, @biomejs/biome@2.5.12, husky@latest

2. **pnpm-workspace.yaml** with packages: ["packages/*", "apps/*"] (monorepo structure for future)

3. **tsconfig.json** with strict mode:
   - target: "ES2022", module: "NodeNext", moduleResolution: "NodeNext", lib: ["ES2022"]
   - strict: true, noUncheckedIndexedAccess: true, exactOptionalPropertyTypes: true
   - declaration: true, declarationMap: true, sourceMap: true
   - outDir: "./dist", rootDir: "./src"
   - baseUrl: ".", paths: { "@/*": ["src/*"] }

4. **.gitignore** with: node_modules/, dist/, .turbo/, *.log, .DS_Store, coverage/, .octate/, ~/.local/share/octate/
  </action>
  <verify>
    <automated>pnpm install && pnpm build 2>&1 | head -20</automated>
  </verify>
  <done>
pnpm install succeeds, TypeScript compiles without errors, dist/ directory created with .d.ts files
  </done>
</task>

<task type="auto">
  <name>task 2: Create Biome configuration and Jest test setup</name>
  <files>
    biome.json
    jest.config.ts
  </files>
  <action>
Create tooling configuration:

1. **biome.json** with:
   - formatter: { indentStyle: "space", indentWidth: 2, lineWidth: 100 }
   - linter: { enabled: true, rules: { recommended: true, correctness: "error", style: "warn", suspicious: "error" } }
   - javascript: { formatter: { semicolons: "always", trailingCommas: "es5", quoteStyle: "single" } }
   - files: { ignoreUnknown: false, ignore: ["dist/", "node_modules/", "coverage/"] }

2. **jest.config.ts** with:
   - preset: "ts-jest", testEnvironment: "node", roots: ["<rootDir>/src"]
   - testMatch: ["**/*.test.ts"], moduleNameMapper: { "^@/(.*)$": "<rootDir>/src/$1" }
   - collectCoverageFrom: ["src/**/*.ts", "!src/**/*.test.ts", "!src/cli.ts"]
   - coverageDirectory: "coverage", verbose: true
  </action>
  <verify>
    <automated>pnpm lint 2>&1 | head -30 && pnpm test -- --passWithNoTests 2>&1 | tail -10</automated>
  </verify>
  <done>
Biome lint passes (no errors), Jest runs without tests and exits cleanly
  </done>
</task>

<task type="auto" tdd="true">
  <name>task 3: Create core domain types, error hierarchy, and Pino logging</name>
  <files>
    src/types/index.ts
    src/errors/index.ts
    src/logging/index.ts
    src/types/index.test.ts
    src/errors/index.test.ts
    src/logging/index.test.ts
  </files>
  <behavior>
    - Test 1: Error classes extend OctateError and have correct exitCode property
    - Test 2: ConfigurationError has exitCode 2, RepositoryError/GitError/ParseError/AnalysisError/ContextError have exitCode 3
    - Test 3: ModelError/ProviderRateLimitError/ProviderTimeoutError/AuthenticationError/QuotaExceededError have exitCode 4
    - Test 4: ValidationError/InternalError have exitCode 5
    - Test 5: All errors accept context object and have human-readable message
    - Test 6: Logger creates child loggers with module context
    - Test 7: Logger supports redaction of sensitive fields
  </behavior>
  <action>
Create core domain files per CONTEXT.md decisions D-24, D-25 and ARCHITECTURE.md:

1. **src/types/index.ts** — Core domain types:
   - Repository: { root: string; gitDir: string; workspaces: Workspace[]; monorepo?: MonorepoConfig }
   - Workspace: { root: string; name: string; packageManager: 'pnpm'|'npm'|'yarn'|'turbo'|'nx'; ignorePatterns: string[] }
   - MonorepoConfig: { type: 'pnpm'|'npm'|'yarn'|'turbo'|'nx'; root: string; workspaces: string[] }
   - FileChange: { path: string; status: 'added'|'modified'|'deleted'|'renamed'; oldPath?: string; diff?: string }
   - ReviewScope: { type: 'working-tree'|'staged'|'commit'|'range'|'branch'; base: string; head: string; files: FileChange[]; diff: string }
   - Export all types and type guards

2. **src/errors/index.ts** — Typed error hierarchy (per D-24, D-25):
   - Base class OctateError extends Error with: exitCode: number, context: Record<string, unknown>
   - ConfigurationError (exitCode: 2)
   - RepositoryError, GitError, ParseError, AnalysisError, ContextError (exitCode: 3)
   - ModelError, ProviderRateLimitError, ProviderTimeoutError, AuthenticationError, QuotaExceededError (exitCode: 4)
   - ValidationError, InternalError (exitCode: 5)
   - All errors: constructor(message: string, context?: Record<string, unknown>)
   - Factory functions for common error scenarios
   - Export all error classes and type guards

3. **src/logging/index.ts** — Pino logger (per STACK.md):
   - createLogger(name: string): Logger — child logger with module name
   - logger: root logger with pretty printing in development
   - Redaction: { paths: ["NVIDIA_API_KEY", "apiKey", "token", "password", "secret"] }
   - Levels: debug, info, warn, error
   - Child loggers inherit redaction
  </action>
  <verify>
    <automated>pnpm test -- src/types/index.test.ts src/errors/index.test.ts src/logging/index.test.ts 2>&1</automated>
  </verify>
  <done>
All tests pass, types and errors compile, logger creates child loggers with redaction
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → File System | User controls CLI args and config files; must validate all paths |
| Config Loader → File System | Reads octate.yaml from user-controlled paths; schema validation required |
| Logger → Console/Files | Debug logs may contain repository content; redaction required |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Config Loader | mitigate | Zod schema validation on all config files; reject unknown keys |
| T-01-02 | Information Disclosure | Logger | mitigate | Redaction of API keys, tokens, secrets in all log levels |
| T-01-03 | Denial of Service | TypeScript Compilation | accept | Build-time only; no runtime impact |
</threat_model>

<verification>
- pnpm install completes without errors
- pnpm build completes with exit code 0
- pnpm lint passes with 0 errors
- pnpm test passes (all unit tests green)
- dist/ contains .d.ts declaration files for all src/ modules
</verification>

<success_criteria>
- Project scaffold complete with all tooling configured
- Core types (Repository, Workspace, MonorepoConfig, FileChange, ReviewScope) defined and exported
- Error hierarchy with 13 error classes matching ROADMAP Phase 6 exit codes implemented
- Pino logger with child logger support and secret redaction working
- All code compiles under strict TypeScript
- Zero Biome lint errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-repository-layer/01-01-SUMMARY.md`
</output>