---
phase: 01-foundation-repository-layer
plan: 05
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - src/cache/identity.ts
  - src/cache/keys.ts
  - src/cache/store.ts
  - src/cache/lru.ts
  - src/cache/index.ts
  - src/cache/identity.test.ts
  - src/cache/keys.test.ts
  - src/cache/store.test.ts
  - src/cache/lru.test.ts
autonomous: true
requirements: ["CACHE-01", "CACHE-02"]
user_setup: []
must_haves:
  truths:
    - "Cache directory created at ~/.local/share/octate/{project-hash}/{indexes,cache,findings,logs}/"
    - "Project identity uses SHA256(repo-root-path + git-remote-url) for namespacing"
    - "Cache keys use SHA256(content) + relativePath + parserVersion + language + configHash"
    - "LRU eviction enforces 500MB max cache size per project"
    - "Bounded concurrency with promise pool for parallel operations"
  artifacts:
    - path: "src/cache/identity.ts"
      provides: "Project identity hashing and cache directory resolution"
      exports: ["computeProjectIdentity", "getCacheDir", "CachePaths"]
    - path: "src/cache/keys.ts"
      provides: "Cache key generation with all required components"
      exports: ["generateCacheKey", "parseCacheKey", "CacheKey"]
    - path: "src/cache/store.ts"
      provides: "File-based cache store with atomic writes"
      exports: ["CacheStore", "createCacheStore"]
    - path: "src/cache/lru.ts"
      provides: "LRU eviction with size tracking"
      exports: ["LRUCache", "createLRUCache"]
    - path: "src/cache/index.ts"
      provides: "Public cache API"
      exports: ["CacheStore", "generateCacheKey", "computeProjectIdentity", "PromisePool"]
  key_links:
    - from: "src/cache/keys.ts"
      to: "src/cache/identity.ts"
      via: "uses project identity for namespacing"
      pattern: "computeProjectIdentity"
    - from: "src/cache/store.ts"
      to: "src/cache/keys.ts"
      via: "uses cache keys for storage"
      pattern: "generateCacheKey"
    - from: "src/cache/lru.ts"
      to: "src/cache/store.ts"
      via: "evicts entries from store"
      pattern: "store.delete"
    - from: "src/cache/index.ts"
      to: "p-limit"
      via: "exports PromisePool for bounded concurrency"
      pattern: "export.*PromisePool"
</threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cache Store → File System | Reads/writes cache files in ~/.local/share/octate/; user controls disk |
| Project Identity → Git Config | Reads git remote URL for identity; repo-controlled |
| Config Hash → Cache Key | Hashes user config; changes invalidate cache automatically |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-16 | Tampering | Cache Files | mitigate | Atomic writes (temp + rename); validate cache key on read; checksum optional |
| T-01-17 | Information Disclosure | Cache Content | mitigate | Cache stores analysis artifacts only; no secrets; findings may contain source snippets |
| T-01-18 | Denial of Service | Cache Growth | mitigate | 500MB hard limit; LRU eviction; octate doctor reports size |
| T-01-19 | Spoofing | Project Identity | mitigate | Hash includes git remote URL; fallback to absolute path + device/inode |
</threat_model>

<verification>
- pnpm test passes for all cache tests
- computeProjectIdentity produces same hash for same repo root + remote URL
- getCacheDir returns correct path: ~/.local/share/octate/{hash}/{indexes,cache,findings,logs}/
- generateCacheKey includes: contentHash, filePath, parserVersion, language, configHash
- CacheStore writes atomically (temp file + rename)
- LRU eviction triggers at 500MB; oldest entries removed first
- PromisePool limits concurrent operations (default: CPU cores)
- octate doctor reports cache size per project
</verification>

<success_criteria>
- Project identity: SHA256(repo-root-path + git-remote-url) per D-10
- Cache key: SHA256(content) + relativePath + parserVersion + language + configHash per D-11
- Automatic invalidation via configHash in key + explicit octate index --force per D-12
- Cache size limit: 500MB per project; LRU eviction on index entries per D-13
- PromisePool provides bounded concurrency (CACHE-02)
- Atomic writes prevent corruption on crash/interrupt
- All cache tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-repository-layer/01-05-SUMMARY.md`
</output>