---
phase: 01-foundation-repository-layer
plan: 06
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-05"]
files_modified:
  - src/cancellation/controller.ts
  - src/cancellation/subprocess.ts
  - src/model/abstraction.ts
  - src/model/types.ts
  - src/model/index.ts
  - src/cancellation/controller.test.ts
  - src/cancellation/subprocess.test.ts
  - src/model/abstraction.test.ts
autonomous: true
requirements: ["CACHE-03", "MODEL-02"]
user_setup: []
must_haves:
  truths:
    - "Single AbortController at ReviewUseCase level propagates to all layers"
    - "Subprocesses (tsc, ruff, pytest, bandit) spawned with signal option auto-kill on abort"
    - "NVIDIA API requests accept AbortSignal and cancel cleanly"
    - "ReviewModel abstraction interface defined with generate(request): Promise<ModelResponse>"
    - "ModelRequest has structured fields for prompt injection protection"
    - "ModelResponse includes findings, usage, metadata"
  artifacts:
    - path: "src/cancellation/controller.ts"
      provides: "AbortController management and propagation"
      exports: ["CancellationController", "createCancellationController", "withCancellation"]
    - path: "src/cancellation/subprocess.ts"
      provides: "Subprocess spawning with AbortSignal support"
      exports: ["spawnWithSignal", "killProcessTree", "SubprocessError"]
    - path: "src/model/types.ts"
      provides: "ModelRequest and ModelResponse type definitions"
      exports: ["ModelRequest", "ModelResponse", "ModelUsage", "ReviewModel"]
    - path: "src/model/abstraction.ts"
      provides: "ReviewModel interface definition"
      exports: ["ReviewModel"]
    - path: "src/model/index.ts"
      provides: "Public model API"
      exports: ["ReviewModel", "ModelRequest", "ModelResponse", "CancellationController"]
  key_links:
    - from: "src/cancellation/controller.ts"
      to: "src/cancellation/subprocess.ts"
      via: "provides signal for subprocess spawning"
      pattern: "controller.signal"
    - from: "src/model/abstraction.ts"
      to: "src/model/types.ts"
      via: "implements ReviewModel interface"
      pattern: "implements ReviewModel"
    - from: "src/cancellation/subprocess.ts"
      to: "node:child_process"
      via: "uses spawn with signal option"
      pattern: "spawn.*signal"
</threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cancellation → Subprocesses | Spawns child processes with signal; OS kills on abort |
| Cancellation → Network | Passes AbortSignal to fetch; NVIDIA request cancelled |
| Model Abstraction → Providers | Interface only; providers implement (Phase 4) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-20 | Denial of Service | Orphaned Processes | mitigate | All subprocesses spawned with signal; process tree killed on abort (D-19) |
| T-01-21 | Resource Exhaustion | Unbounded Model Requests | mitigate | AbortController passed to fetch; timeout enforced at provider level (Phase 4) |
| T-01-22 | Tampering | ModelRequest Structure | mitigate | Structured fields enforce trusted/untrusted separation (D-15, D-34) |
| T-01-23 | Spoofing | ModelResponse | mitigate | Schema validation in Phase 4; interface defines expected shape only |
</threat_model>

<verification>
- pnpm test passes for all cancellation and model tests
- CancellationController creates AbortController, tracks child signals
- withCancellation wraps async operations and checks signal
- spawnWithSignal passes signal to child_process.spawn (Node 18+)
- killProcessTree terminates process group on abort
- ReviewModel interface: generate(request: ModelRequest): Promise<ModelResponse>
- ModelRequest has: systemPolicy, reviewTask, projectRules, repoMetadata, diff, context[], diagnostics[], outputSchema
- ModelResponse has: findings[], usage{promptTokens,completionTokens,totalTokens}, model, latencyMs, rawResponse?, finishReason
- Two provider types planned: LocalNvidiaProvider, HostedProvider (D-17)
</verification>

<success_criteria>
- Single AbortController at ReviewUseCase level propagates to Repository, Analysis, Intelligence, Model, Review layers (D-18)
- Subprocess cancellation via { signal } option (Node 18+) auto-kills on abort (D-19)
- NVIDIA API requests accept same AbortController.signal for unified cancellation (D-20)
- ReviewModel abstraction interface defined per D-14, D-15, D-16
- ModelRequest structured fields enforce prompt injection protection (trusted vs untrusted separation)
- ModelResponse includes all required metadata (usage, latency, finishReason)
- All cancellation and model tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-repository-layer/01-06-SUMMARY.md`
</output>